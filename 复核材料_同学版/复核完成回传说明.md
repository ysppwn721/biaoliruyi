# 回传前检查

- 共 280 行，每行 `reviewer_decision` 都已填写。
- `link` 行的 `reviewer_refs` 不为空，且 ID 来自同一行 `facts_json`。
- `abstain` 行的 `reviewer_refs` 为空。
- 每行 `reviewer_category` 都是 `ordinary`、`ambiguous` 或 `adversarial`。
- 有判断依据的行填写 `reviewer_note`。
- 不修改 `claim_id`、`claim_text`、`facts_json`、`source_file` 和 `source_location`。

回传文件名建议：`人工复核清单_同学A_已完成.csv`。
