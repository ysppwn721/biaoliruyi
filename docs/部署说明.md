# 公网部署说明（2026-09-26 实际状态）

> 目标：满足大赛「作品应具备公网内的联通性且能流畅运行」的要求。
> 本文记录**当前线上真实运行的架构**，与早期方案（Caddy 同域直出）不同，请以本文为准。

## 一、当前线上地址

| 项目 | 地址 | 载体 |
| --- | --- | --- |
| 产品首页 / 下载页 | `https://zhilian.space/` | Cloudflare Worker `biaoliruyi`（静态资源） |
| 在线演示（真实工作台） | `https://demo.zhilian.space/` | Cloudflare Tunnel → 云服务器 FastAPI |
| 后端健康检查 | `https://demo.zhilian.space/api/health` | 需 Basic 认证，返回 401 表示保护生效 |
| 额度查询 | `https://demo.zhilian.space/api/quota` | 只读，返回今日剩余模型调用次数 |
| 测试账号 | 用户名 `zhilian`　密码 `zhilian2026` | 已按大赛要求公开在站点与设计文档 |

`https://zhilian.space/demo/` 保留为**跳转页**，会自动转到 `demo.zhilian.space`，
以便早期文档、书签与二维码里的旧链接仍然可用。

## 二、为什么把静态页和后端分开

```
zhilian.space        → Cloudflare Worker     静态站 + 全部安装包（不占服务器带宽）
demo.zhilian.space   → Cloudflare Tunnel  →  8.141.119.159:8765（FastAPI）
```

好处：

1. **服务器零公网入站端口**：安全组维持默认即可，所有流量由 cloudflared 出站建立；
   应用只监听 `127.0.0.1:8765`，公网无法直接访问。
2. **25 MB 的 Windows 安装包不消耗服务器带宽**，全部由 Cloudflare 边缘分发。
3. 静态站更新与后端更新互不影响。

## 三、云服务器

| 项目 | 值 |
| --- | --- |
| 实例 | 阿里云 ECS `i-2ze2qa3jlagsvbwo6ye7`，华北2（北京），`cn-beijing` |
| 公网 IP | `8.141.119.159`（私网 `172.24.162.189`） |
| 系统 | Ubuntu 22.04.5 LTS，2 核 / 3.6 GB / 40 GB |
| 代码 | `/opt/zhilian/biaoliruyi`（git 浅克隆） |
| 运行时 | `/opt/zhilian/venv`（Python 3.10 + requirements.txt） |
| 数据 | `/opt/zhilian/data`（项目状态、`quota.json`） |
| 密钥配置 | `/opt/zhilian/biaoliruyi/.env`（权限 0600，含 `DEEPSEEK_API_KEY`，**不进仓库**） |
| 模型 | `/opt/zhilian/models/bge-reranker-v2-m3-onnx-int8`（可选，缺省自动降级） |

**两个 systemd 服务**（均 `enabled`，开机自启、崩溃重启）：

| 单元 | 作用 |
| --- | --- |
| `zhilian.service` | 运行 FastAPI，监听 `127.0.0.1:8765` |
| `cloudflared.service` | 固定隧道 `zhilian-demo` 的连接器 |

```bash
systemctl status zhilian cloudflared
journalctl -u zhilian -n 50 --no-pager
journalctl -u cloudflared -n 50 --no-pager
```

**更新版本：**

```bash
cd /opt/zhilian/biaoliruyi
git fetch --depth 1 origin main && git reset --hard origin/main
/opt/zhilian/venv/bin/pip install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements.txt
systemctl restart zhilian
```

> 用 `git reset --hard origin/main` 而不是 `git pull`：浅克隆下 `pull` 会因历史分叉而失败。

## 四、隧道

- 隧道名 `zhilian-demo`，ID `b1a4132c-0645-4366-ac0a-dd7470234b86`
- 公开主机名：`demo.zhilian.space` → `http://localhost:8765`（兜底 `http_status:404`）
- 连接器由 `cloudflared service install <token>` 安装为系统服务；token 只存在于
  该单元配置中，不在本仓库、不在文档里。

## 五、模型调用限额（服务端付费的护栏）

演示环境用服务端密钥替使用者付费，而测试账号按大赛要求公开，因此必须限流：

| 变量 | 默认 | 含义 |
| --- | --- | --- |
| `ZHILIAN_QUOTA_PER_CLIENT` | 60 | 单个访客每日模型调用上限 |
| `ZHILIAN_QUOTA_GLOBAL` | 600 | 全局每日上限 |

- 访客标识优先取 `CF-Connecting-IP`；演示位于 Cloudflare 之后，取 socket 地址会把
  所有访客算成同一个边缘节点。
- 超限**不报错**，自动降级为规则模式，并在项目审计里记一条「模型额度用尽」。
- 计数按自然日清零，落在 `/opt/zhilian/data/quota.json`；文件损坏或不可写时按零消耗
  处理，限额模块本身不会影响演示。
- 工作台右上角与「请求 DeepSeek 关联建议」「运行智能体」弹窗都会显示今日剩余次数。

## 六、为什么保留密码保护

大赛要求「软件若设置了用户名和密码，必须在显著位置注明测试账号和密码」。
保留保护的价值：

1. **不被爬虫和陌生人误用**——演示环境开放写入（可创建项目、下载文件）；
2. **符合大赛要求**——账号已在站点和设计文档中公开；
3. **演示可复现**——评委用同一账号进入，看到一致的环境。

## 七、上线前检查清单

- [x] `https://zhilian.space/` 可打开，下载链接全部 200
- [x] `https://demo.zhilian.space/` 可打开真实工作台
- [x] 用测试账号可登录并跑通完整闭环
- [x] 测试账号已写进《软件设计文档》显著位置
- [x] 服务器无公网应用端口暴露
- [ ] 演示环境有效期覆盖至 **11 月 14 日决赛**之后
- [ ] 准备**本地离线备用环境**（决赛现场网络故障时使用）

## 八、已注意到的风险

1. **模型不随包分发**：560 MB 的 ONNX reranker 不在仓库（GitHub 单文件限 100 MB），
   需手工上传；缺失时自动降级为规则 + API，功能不缺。
2. **数据持久化**：`/opt/zhilian/data` 必须保留；建议开每日备份（见 `deploy/backup.sh`）。
3. **密钥轮换**：`DEEPSEEK_API_KEY` 曾出现在沟通记录中，比赛结束后应到控制台轮换。
4. **突发性能实例**：不要用 t5/t6 之类共享型规格，ONNX 推理会打满 CPU 并被限频。

## 九、备选部署方式（compose + Caddy）

仓库 `deploy/` 仍保留一套 Docker Compose + Caddy 的同域方案，适用于「服务器直出整站
并自行申请 HTTPS」的场景。它与当前线上方式二选一即可，注意两者都会占 80/443。
