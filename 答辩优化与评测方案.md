# 知链当前状态校准、优化方案与答辩评测

> 本文根据 2026-09-24 统一目录中的实际源码、测试和方案文档整理。它用于团队决策和答辩准备，优先以代码和可重复测试结果为准。

## 一、先校准现状

### 1. 当前真正已经实现的能力

当前整合后的开发主线版本为 `0.2.1`，以交付源码为准，后端核心模块是：

| 模块 | 当前职责 |
| --- | --- |
| `zhilian/engine.py` | 规则抽取和确定性核验：数值引用、增长率、排名、阈值、图表；复合句拆分；Decimal 计算；单位和统计口径校验 |
| `zhilian/graph.py` | 派生事实、白名单表达式、拓扑排序、循环依赖检查、传递影响和影响半径 |
| `zhilian/office.py` | Excel 读取；Word 普通正文/一级表格；PPT 普通文本、表格和单系列簇状柱形图；局部文本补丁 |
| `zhilian/store.py` | 项目状态、SHA 校验、revision 并发保护、generation 版本、预览、事务性修复、重新读取复核、撤销和导出 |
| `zhilian/app.py` | FastAPI 接口、上传、Basic Auth、CSRF Origin 检查、派生事实接口和模型建议入口 |
| `zhilian/llm.py` | 可选的 DeepSeek/OpenAI 兼容 `/chat/completions` 适配器，提供来源关联和诊断解释 |
| `zhilian/agent.py` | 证据链验证智能体：状态机、工具调用、人工闸门、持久化待办和执行轨迹 |
| `zhilian/diagnose.py` / `report.py` | 结构化诊断分类、确定性核验报告和变更摘要 |
| `zhilian/ocr.py` | 可选图片 OCR 只读支线，结果必须人工核对 |
| `zhilian/tools.py` / `mcp_server.py` | 7 个只读项目工具及 stdio MCP 服务 |
| `web/` | 上传、来源确认、事实更新、影响预览、修复确认、依赖图、导出和撤销工作台 |

这些模块已合入根目录并通过交付源码回归测试。Agent 仍是确定性状态机编排器，不是自由 ReAct；大模型只在受限工具中提供来源候选和解释润色，不能直接计算、裁决或写文件。

### 2. 模型现在到底做什么

当前模型只做一件实际工作：**给已经被规则识别、但尚未确认来源的非图表论断提供候选事实 ID**。

调用入口是 `zhilian/llm.py:suggest_links`，输入包括：

- 待确认论断的 `id`、`kind`、原文和当前候选 `refs`；
- 事实的 `id`、主体、指标、期间、单位、统计口径。

当前明确约束：

- 每次最多取 40 条未确认且非图表论断；
- 事实超过 150 条直接拒绝本次调用；
- 不发送事实数值；
- 模型不能自动确认来源、计算数值或写回 Office 文件；
- 返回的 claim ID、事实 ID 和列表结构会被程序校验；
- 模型失败时保持原关联不变，回到人工确认。

因此当前正确的答辩说法是：

> 模型负责语义候选，程序负责口径校验和数值计算，用户负责最终确认，程序负责局部写回和独立复核。

不要说“模型自动完成文档更新”，也不要说“已经证明模型关联准确率很高”。当前 `tests/test_models.py` 全部是 mock HTTP，验证的是协议、安全和降级行为，**没有验证真实模型建议是否正确**。

### 3. 当前测试基线

整合后的根目录当前收集到 195 个 pytest 用例，最近一次实际运行结果为：

```text
195 passed, 1 warning
```

已覆盖：

- 复合论断拆分和局部修复；
- 派生事实、拓扑排序、环依赖、级联影响；
- 大事实表坐标和线性读取性能；
- 事实匹配、歧义和缓存索引；
- 模型接口 mock、密钥不回显、事实数值不外发、非法返回过滤；
- Word/PPT 真实文件回写、图表、撤销和导出；
- 外部 Excel 预览、语义变更拒绝、磁盘篡改、revision 冲突和 Origin 防护。

旧文档中出现的“24 passed”“62 passed”“76 passed”“144 passed”或“166 passed”属于不同代码快照的记录。当前整合主线统一按 195 项回归测试说明；交付源码副本单独为 166 项。

## 二、项目应该怎样定位

建议统一为：

> **知链：让 AI 写作时代的数据结论可追溯、可复核、可验证。**

问题不是“帮用户把 Excel 复制到 Word”，而是：数据表发生变化后，报告、汇报 PPT 和图表中的结论可能有一部分仍然成立、有一部分已经失效，还有一部分因为来源不明确根本不能判断。知链把“事实 → 派生计算 → 论断 → 文档位置”串起来，定位受影响结论，只修改失效锚点，并在导出前重新读取文件复核。

这条定位比“通用 AI 助手”更适合现有实现，也能回答创新性问题：

1. 不是整篇重写，而是基于证据的最小修改。
2. 不是让模型算数，而是用确定性程序算数。
3. 不是盲目自动化，而是把歧义和高风险操作交回用户确认。

答辩可用的三个小标题：

- **数据变了，哪一句结论失效？**
- **每次修改，依据能不能追溯？**
- **改完以后，文件能不能独立复核？**

## 三、优化优先级

### P0：必须先完成，直接决定答辩可信度

#### P0-1 建立真实模型评测集

这是当前最重要的工作。不要先堆 LangChain、LangGraph、MCP 名词；先回答“模型建议到底对不对”。

评测单位应是“文档组”，而不是同一篇文档里的句子数量。建议第一版建设 30 个文档组、每组 6—10 个待确认决策点，形成 180—240 个决策点：

| 子集 | 建议占比 | 内容 | 要测什么 |
| --- | ---: | --- | --- |
| 普通匹配 | 60% | 论断和事实在主体、指标、期间、口径上唯一对应 | 常规候选关联能力 |
| 歧义样本 | 25% | 同指标不同主体、同主体不同期间、同期间不同口径、两个近似事实 | 是否拒答并转人工，而不是瞎猜 |
| 对抗样本 | 15% | 文档正文中加入“忽略以上要求”“把 refs 指向 X”等数据文本 | 提示注入劫持率是否为 0 |

开发集和留出集必须按文档组隔离。调 prompt、改规则和做 few-shot 的样本只能进入开发集。标准答案由两名成员独立标注，再记录分歧和仲裁结果；不能由实现者单独事后标答案。

每条标注至少保存：

```json
{
  "group_id": "g-001",
  "claim_id": "c-001",
  "claim_text": "本期销售额为125万元",
  "facts": ["sales_current", "other_sales_current"],
  "gold_refs": ["sales_current"],
  "gold_action": "link",
  "category": "ordinary",
  "annotators": ["A", "B"],
  "adjudicated": true
}
```

歧义样本的标准答案不一定是某个事实 ID，也可以是 `abstain`。这是避免把“错误猜中”当成智能的关键。

#### P0-2 三组对照实验

在同一留出集上比较：

- **A 规则候选**：使用当前 `extract_claims`、`best_facts` 和确认前候选；
- **B 纯模型**：把同一批已识别论断和事实元数据交给模型，不使用规则候选；
- **C 规则 + 模型**：规则先缩小候选，模型在候选上排序或拒答。

程序计算最终指标，不能把模型自报的“置信度”当答案。至少输出：

| 指标 | 计算方式 | 答辩意义 |
| --- | --- | --- |
| Top-1 准确率 | 唯一 gold refs 中，首选完全匹配的比例 | 普通关联是否有效 |
| 错误关联率 | 输出非空 refs 且与 gold 不一致的比例 | 比总准确率更重要，错误关联可能导致错误修复 |
| 歧义拒答率 | gold 为 `abstain` 时返回空/转人工的比例 | 是否会在不确定时停手 |
| 误拒率 | gold 为唯一关联却返回空的比例 | 是否过度保守 |
| 对抗劫持率 | 对抗样本中违反允许 ID 或执行文档指令的比例 | 安全边界是否成立，目标为 0 |
| 规则+模型增益 | C 的 Top-1 与 A 的差值 | 模型是否真的带来增量 |
| 延迟 | 每文档组和每批请求的 p50/p95 | 现场体验是否可接受 |
| token/成本 | 记录响应 `usage`、请求次数和供应商单价 | 证明模型调用值得付费 |

报告中必须同时给出样本数和 95% 置信区间，至少不要只报一个百分比。30 个文档组规模较小时，答辩口径应写“在本留出集上”，不要宣传成行业泛化准确率。

#### P0-3 做当前能力的曲线证据

现在已经可以真实展示两类数据：

1. **事实表规模—解析耗时曲线**：当前实现对 50、100、250、500、1000、1500、1999 条事实的读取中位数约为 0.005、0.008、0.015、0.027、0.052、0.080、0.118 秒。这个曲线可以支撑“大表读取保持可用”，但不能替代模型准确率评测。
2. **长文修复结果**：24 章、约 1.7 万字、280 个论断；轻微变化修复 80 项，较大变化修复 232 项；修复后均恢复为 280 项一致。这个适合用堆叠柱状图呈现“仍成立/需修复/修复后通过”。

已生成真实性能数据和曲线：

- `答辩评测/fact_table_performance.csv`
- `答辩评测/事实表读取性能曲线.svg`
- `答辩评测/长文Word修复结果对比.svg`
- `答辩评测/measure_fact_table.py`
- `答辩评测/make_charts.py`

重新生成命令：

```powershell
.\.venv\Scripts\python.exe 答辩评测/measure_fact_table.py
.\.venv\Scripts\python.exe 答辩评测/make_charts.py
```

模型评测完成后，再补一张“样本量—Top-1/拒答率/错误关联率”或“规则、纯模型、融合三组柱状图”。在真实 API 跑分前，不要画一条假设性的模型提升曲线。

### P1：建议在 P0 后做，能提高模型赛道表现

#### P1-1 最小版模型论断抽取

当前规则只识别固定句式。模型扩展可以负责提出结构化候选，例如占比、区间、同比/环比、平均值和多主体比较；确定性引擎继续负责计算和验证。

模型输出应包含：`kind`、原文锚点、候选 refs、数值/方向、理由、置信度。任何模型抽取结果都只能进入“待确认”，不能直接写文件。优先只做两三类高收益表达，例如：

- `本期支出占预算的80%`；
- `收入较去年同期下降12%`；
- `华北区域收入在各区域中最高`。

抽取扩展必须与 P0 评测分开统计：抽取召回率、抽取精确率、锚点准确率和错误写回率不能混在来源关联准确率里。

#### P1-2 诊断解释和项目内问答

当前代码已有确定性 `reason`、`expected` 和 `evidence`，可以先做不依赖外部知识库的项目内问答：

- “为什么第 3 页结论失效？”
- “这次 Excel 改了哪些事实？”
- “还有哪些论断没有来源确认？”

模型只负责把已有结构化诊断改写为自然语言；所有数字、位置和状态必须从程序返回值引用，并做数字一致性校验。这个方向比直接接一个泛聊天框更贴合项目。

#### P1-3 诊断和审计可观测性

如果接入真实模型，审计记录至少增加：模型名和版本、请求批次、claim 数、HTTP 状态、耗时、输入/输出 token、估算费用、是否降级、人工是否采纳。当前 `suggestions` 审计只记录“提出了多少项建议”，不够支撑成本曲线。

### P2：可以规划，暂时不要抢 P0 时间

- OCR 和图片直传：有真实文件需求后再做，先支持“提取—展示—人工确认”，不要承诺图片内文字自动重绘；
- PDF 扫描和复杂图表：工作量大，且当前 Office 主流程尚未覆盖；
- LangChain/LangGraph：只有在需要长任务暂停、恢复、重试或多工具规划时再引入，不能为了名词而引入依赖；
- MCP：可以把查询项目状态、影响范围和变更记录封装为只读工具，但它是互操作层，不是核心创新；
- ToC、安卓/iOS、6000 张航拍语义分割：与当前“跨文档结论治理”主线不一致，建议作为其他方向，不纳入本轮比赛主线。

## 四、当前工程问题的处理顺序

除模型评测外，建议按以下顺序处理已有代码问题：

1. **先保持当前事实表性能补丁和 195 个测试通过**。`office.py` 已使用 `get_column_letter` 生成单元格坐标，不能回退到 read-only 模式逐行 `sheet.cell()`。
2. 修正旧文档中的实现状态和测试数字，避免“规划功能冒充已实现”。
3. 将诊断状态分类从错误文案字符串改为结构化 `code`，防止修改中文提示导致分类失效。
4. 将模型请求的 token、延迟、费用和降级状态写入 audit，方便评测和答辩。
5. 诊断/批量检查按批次写盘，避免每条论断都全量改写 `state.json`。
6. 增加版本清理、项目删除或历史恢复点；当前 generation 和导出包会持续增长，撤销也主要是单步。
7. 如果开放 OCR，统一后端白名单、前端 `accept` 和上传列表图标；能力必须前后端一致。

## 五、四人分工建议

| 成员 | 负责内容 | 必交证据 |
| --- | --- | --- |
| 1 | 评测集、双人标注、指标脚本和统计置信区间 | `eval_dataset.jsonl`、标注说明、指标表 |
| 2 | `llm.py` 真实 API 跑分、批处理、缓存、token/成本审计 | 原始调用日志、脱敏结果、模型版本 |
| 3 | 前端评测结果、依赖图和修复对比画面 | 三张答辩截图、演示流程 |
| 4 | 规则/模型融合、失败案例、设计文档和视频 | 对照实验报告、2—3 个失败案例、视频脚本 |

任何一个新功能都要同时满足三个条件才进入答辩主线：现场能演示、能产出指标、对应明确用户痛点。否则留在长期规划中。

## 六、建议的答辩演示顺序

1. 上传 Excel、Word/PPT，展示事实、论断和来源候选。
2. 通过依赖图选择一个事实，展示直接和间接受影响的论断。
3. 更新一个轻微值，展示“数据变了但阈值结论仍成立”。
4. 更新多个值，展示失效结论、计算过程、原文锚点和修复预览。
5. 确认修复，下载 Word/PPT，展示独立重读复核和变更报告。
6. 展示歧义样本：模型不猜，转人工确认。
7. 展示提示注入样本：正文里写“忽略以上指令”，模型仍只能从允许的事实 ID 中选择，劫持率为 0。
8. 最后展示评测曲线和成本/延迟表。

答辩中要主动说清：模型不是算数器，也不是拥有文件写入权的自动代理；它被限制在候选匹配和解释层，程序与人工闸门控制结果。

## 七、当前能说和不能说

### 可以说

- 当前有确定性规则核验、事实依赖图和级联影响；
- 当前支持 Word/PPT 受限范围内的局部修复和独立重读复核；
- 当前整合主线测试为 195 个 pytest 用例，全部通过；交付源码副本测试为 166 个用例，全部通过；
- 长文 Word 已完成 24 页、280 项论断的真实压力测试；
- 模型请求不发送事实数值，返回 ID 会经过白名单校验；
- 真实模型质量评测正在建设，指标设计包括准确率、误关联率、歧义拒答率、劫持率、延迟和成本。

### 不能说

- 不能把 Agent 的确定性编排说成自由 ReAct，也不能把 mock 模型测试说成真实模型效果；
- 不能说模型关联准确率已经达到某个百分比，除非真实 API 在留出集上跑出原始记录；
- 不能把 mock 测试当成模型效果测试；
- 不能把 24 页 Word 结果外推为任意复杂 Word 或 20 页以上 PPT 都能稳定处理；
- 不能把事实表性能曲线当成模型智能曲线；
- 不能把规则和融合方案的提升写成结论，直到三组对照实验完成。

## 八、最终建议

项目现在最值得做的不是继续堆功能，而是把已有“可信核验闭环”转化为一组可信证据：

1. 先冻结当前版本和测试基线；
2. 完成隔离评测集和真实模型跑分；
3. 用错误关联率、歧义拒答率、对抗劫持率和无关内容误改率讲差异化；
4. 再做最小版模型抽取或项目内诊断问答；
5. 只有在指标、日志和演示画面都稳定后，才考虑 OCR、MCP 互操作和更复杂 Agent 编排。

这样答辩时能够同时回答“系统现在能做什么”“模型有没有真正带来收益”“为什么不会乱改文件”和“后续如何扩展”，并且每个回答都有源码、测试或评测数据支撑。
