# 知链技术深化路线

> 评审日期：2026-09-18 ｜ 对象：`zhilian/` v0.2.0 ｜ 距官网最终提交（10/8）20 天
> 结论：**技术不空洞，反而比原政策方向高一个量级。真正松的是定位、部署和推理链三处。**

## 一、先说结论：这不是"缺功能"，是"缺三样结构"

我读了 `zhilian/engine.py`（308 行）、`office.py`、`store.py`、`README.md`、`项目交接文档.md`、`知链项目方案_队内讨论版.docx`，并跑了回归测试。

### 已有的真本事（这些不是包装，是可验证的工程）

| 能力 | 证据 |
|---|---|
| 确定性核验，不用模型算数 | `engine.py` 用 `Decimal` + 显式单位换算，注释明确 "No eval, generated code or model arithmetic" |
| 五类论断抽取 + 精确文本锚点 | 数值引用/增长率/排名/阈值/图表，复合句逐项拆分且不遮蔽 |
| 口径一致性强制 | 增长率要求主体、指标、口径、上期本期顺序全一致，否则转人工；零/负基期拒绝计算 |
| run 级 OOXML 补丁 | Word 按 run 打补丁保留格式；PPT 重建原生单系列柱形图数据系列 |
| 事务性版本管理 | SHA256 校验 + revision 并发保护 + 新 generation 写入失败即删 |
| 独立复核 | 修复后重新读取 Office 文件，验证选中论断已一致且其他正文未变 |
| 长文实测 | 24 章／1.7 万字／337 段／280 项论断；232 项失效全部修复，48 项正确保留；24 页排版未变；18 个 ZIP 部件未动 |
| 回归测试 | **`pytest -q` → 24 passed in 3.24s**（我本机实测通过） |
| 无模型可运行 | 未配置任何模型时规则模式完整可用 → 现场断网也不塌 |

再往下看，`office.py` 里那段"按字符范围逆序替换，避免前面的替换改变后面锚点"的处理，是所有做文档补丁的人都会踩的坑，你们绕过去了。这类细节比功能列表更能说明水平。

### 三处真正的缺口

**缺口 1：你们自己的方案文档把创新点写成了"跨文档证据依赖图"，但代码里没有图。**
`claims.refs` 是扁平引用，事实之间没有依赖关系，因此**没有传递性**：事实 A 由事实 B 计算得出时，改 B 不会级联到引用 A 的结论。而"依赖图"是整套创新主张的地基——地基没浇，上面三层（影响定位、最小修复、对照实验）都还悬空。

**缺口 2：论断抽取纯正则，模型层只做"关联建议"，且未接真实 API、无质量评测。**
这是评委最容易说"不够智能"的地方：纯正则 + 已确认关联的演示，看起来像"带确认的查找替换"。你们自己在方案里定的"模型输出结构化检查草案、程序执行检查"这条路线，代码里还没落地。

**缺口 3：定位（社会价值/应用背景）与部署（公网）都还没做。**
- 部署：`README` 自己写着"公网服务器、域名、HTTPS 和真实模型额度尚未购买或部署"。而大赛方案写的是"**仅限在某个专网或校园网上访问而无法通过公网访问的作品……将被视为无效参赛作品**"。这是作废项，不是扣分项。
- 定位：当前的描述是"跨文档结论验证与增量修复原型"——**是工具名，不是问题陈述**。评审考核"思想性、时代感、社会价值和应用背景"，这四项现在都拿不到分。

## 二、优先加什么：三件事

### P0-1　重新定位：一句话把社会价值讲出来

现在评委听到的是"一个文档校验工具"；你们要让他们听到的是"**AI 生成内容时代的可信性治理**"。

推荐口径：

> **知链——让 AI 写作时代的数据结论始终可追溯、可复核、可验证。**

叙事链条要顺下来：大模型让文档产出速度提高，于是"文数不一致"从偶发变成常态；源数据一变，报告、汇报、图表里的结论大面积失效，而**人和模型都不知道哪一句已经错了**。知链建立"事实 → 计算 → 结论 → 文档位置"的可追溯关系，定位失效结论、给出带证据的最小修改、改完再做独立复核。

这条叙事有个额外好处：它把项目的靶子对准了**大模型自身的不可靠**——而这正是本届赛道的核心议题。往届拿一等奖的《WTO 智询》靠的就是"全流程透明化、来源可追溯"，同样的能力用在文档治理上，评委池认这个。

**顺带一个很有说服力的自证**：用知链去校验你们自己的参赛材料——设计文档里的数据与 Word 报告、演示视频里的数字一致。答辩时这一句比任何功能列表都有力。

### P0-2　推断链数据库 / 依赖图（最硬的差异化，也最缺）

按难度分三步走，**至少完成第一步**：

**第一步：事实间依赖 + 传递闭包**
- 事实增加 `derived_from`（由哪些事实计算）与 `expr`（受限表达式）；
- 数值变化后，重算所有派事实，再检查引用它们的论断；
- 演示场景：`毛利 = 收入 − 成本`、`合计 = 各分项之和`、`增长率 = (本期 − 上期)/上期`、`预算执行率 = 支出/预算`。

**第二步：影响半径与前缀剪枝**
- 从变更事实出发做图遍历，只对可达的 claims 做检查，避免全量扫描；
- 输出"影响半径"这个指标（受影响节点数 / 全图节点数），它同时是性能优化和答辩亮点。

**第三步：图的健全性检查（最能体现技术含量）**
- 用 `networkx` 对依赖图做**拓扑排序**，检测**环依赖**——"支出由预算算出、预算又由支出算出"这类循环引用，业务上意味着文档自相矛盾，系统应当直接报错而不是算出一个数；
- 检测**悬挂引用**（引用了不存在的事实）与**口径冲突**（同主体同指标同期间但 scope 不同）；
- 用 `ortools`（可选）做**最小改动集**：多个事实同时变化、且若干修改之间存在约束时（同一句里两处数字不能互相打架），求一个改动最少、复核成本最低的修复方案。

这一步把项目从"规则系统"抬到"图算法 + 约束优化"，而且**完全自研、不需要训练模型、20 天内可交付**。

### P0-3　把对照实验做出来（把"我做了系统"升级为"我验证了假设"）

这是投入产出比最高的一件事，也是《知链项目方案》第 5 节已经写好、但一行没跑的东西。

- **测试集**：20—30 组文件（数据表 + 报告 + PPT），每组 3 个独立变化案例 → 60 个变更案例；两人独立标注关键答案，再核对分歧；
- **三基线对比**：① 关键词/数值查找替换；② 大模型读整组文件后生成修改；③ 知链（依赖图 + 受限验证 + 局部修复）。基线要在相同内容、相同模型版本、明确资源预算下跑，记录真实调用成本；
- **消融**：依次移除口径校验、可执行结论检查、依赖图，看性能如何变化；
- **统计单位用"文件组"**，不能把同一文件里的多个相似句子当成独立证据；
- **交付**：一张指标表 + 2—3 个失败案例 + 成本/延迟数据。

指标口径沿用方案文档：影响定位查准率/查全率、失效结论检出率、修复成功率、**无关内容误改率**、复核成本与运行成本。

**"无关内容误改率"是你们最锋利的指标**——整篇重新生成必然误改大量无关段落，而你们能证明自己不改。这一条数字打出来，就差一个奖杯了。

### P1　模型层真正上场（但保持"模型建议、程序裁决"）

- 让模型输出结构化草案：`{类型, 文本锚点, 候选 refs, 依据}`，**检查权和文件写入权仍归程序**——这条边界不能松，它是你们可信度的来源；
- 两种模式都给：正则（快、零成本、可离线）vs 模型（覆盖复杂表述）。演示时切换对比，正好展示"什么时候值得用模型"；
- 用一个小测试集报告**模型相对正则基线的增量**（而不是只报绝对值），这样"模型有没有用"是有证据的，不是信仰。

## 三、明确不要做的（避免把优势摊薄）

| 不要做 | 理由 |
|---|---|
| 多人实时协同 | 方案已标"暂缓"，且与核心问题无关 |
| 扫描 PDF / 图片文字 | 成本极高，且复制现成 OCR 能力不算原创 |
| 数据库/API 直连取数 | 会稀释"跨文档一致性"这个唯一焦点 |
| 自建期刊/引文库、日历提醒 | 都是外围功能，不解决"哪句错了" |
| 复杂图表族、组合对象、宏 | 无底洞，且违反方案自己划的能力边界 |

**判断标准**：每加一个功能，先问它能不能被现场演示、能不能产生一个可报告的指标、能不能对应真实用户痛点。三者缺一，就是减分项而不是加分项。

## 四、演示必须让"看不见的东西"变可见

现状风险：文档补丁在视觉上不明显，评委可能"看不出发生了什么"。**必须做三个画面**：

1. **依赖图视图**——改动一个事实，图上受影响的节点高亮扩散。让"影响半径"这件事一眼可见。
2. **修复预案对比视图**——左：整篇重新生成会改掉哪些无关段落（红）；右：知链只改哪几句（绿）。**这个对比画面本身就是答辩的核心论据。**
3. **独立复核报告视图**——把"改完重新读回文件再验一遍"这个隐形动作做成一张可看的报告：选中论断已一致、其余正文未变、排版部件未动。

## 五、20 天冲刺（以 10/8 提交为准，倒排）

| 日期 | 里程碑 |
|---|---|
| 9/18—9/19 | 确认校内提交状态与队员最终名单；**立即启动公网部署**（买域名/服务器或容器托管 + HTTPS 反代） |
| 9/20—9/24 | **事实依赖图 + 传递闭包**跑通（P0-2 第一步）；`derived_from` 与影响半径输出 |
| 9/25—9/28 | 模型抽取模式接入国产模型 API，与正则双跑并列展示 |
| 9/29—10/1 | 测试集与三基线对照实验开跑；产出指标表与失败案例 |
| 10/2—10/4 | 依赖图视图 + 修复对比视图 + 复核报告视图 |
| 10/5—10/7 | **冻结**：演示脚本彩排、设计文档 PDF 定稿、源代码原创标注、AI 使用台账、演示数据版本冻结 |
| **10/8—10/10** | **官网最终提交** |

## 六、交付物与合规（逐项自查）

- [ ] **公网可访问**，服务器/域名/证书有效期覆盖到 11/14；**准备测试账号并在设计文档显著位置注明用户名和密码**（`ZHILIAN_ACCESS_PASSWORD`，用户名固定 `zhilian`）
- [ ] 作品简介 3 张截图：建议就取"依赖图视图/修复对比视图/复核报告视图"
- [ ] ≤400 字简介、队长电话邮箱
- [ ] **软件设计文档 PDF（官网模板）**：写清需求分析、依赖图数据模型、受限验证机制、失败处理、操作步骤
- [ ] 完整源代码（**标注原创部分**；`third_party/licenses/` 已齐备，保留声明）
- [ ] 软件安装包：Web 系统可提交访问入口说明 + 二维码；Dockerfile 已具备，建议实测容器可跑并写入文档
- [ ] 演示视频 MP4 ≤5 分钟、≤150MB
- [ ] 模型名称 + 版本号 + **AI 编写代码比例**（README 已写明"当前代码由 AI 辅助生成"，台账需细化到模块）
- [ ] 把项目纳入 Git：`.gitignore` 需排除 `.venv/`、`.zhilian/`、`.codex_artifacts/`、`.env`、`_tmp_plan.pdf`、测试产物与 `__pycache__`
- [ ] **可复现性验证**：在一台干净机器或容器里按文档跑通 `pytest` 与最小闭环（交接文档第 12 节自定的完成标准）

## 七、预计被问什么（答辩准备）

| 问题 | 应答要点 |
|---|---|
| 为什么不让大模型整篇重写？ | 用"无关内容误改率"数字回答；整篇重写必然误改，且无法逐条给证据 |
| 模型算的数可靠吗？ | 算数不由模型做：`Decimal` 确定性计算，模型只提候选关联、由人确认 |
| 怎么保证没漏改？ | 未被锚点覆盖的原文单独列出（`unmatched_segments`），不因同段另有一条通过就被默认为正确 |
| 准确率多少？ | 报三基线对照结果，并说明统计单位是文件组 |
| 模型是必需的吗？ | 不是——正则模式零成本可离线跑；模型用于覆盖复杂表述，增量有实测 |
| 改完排版会不会乱？ | run 级补丁 + 24 页逐页核验 + 18 个部件未变；同时说明这只是受支持范围，不承诺任意排版保真 |
