# 下一阶段执行清单

## 当前进度（2026-09-25）

- 已完成 CUDA/CPU 本地 reranker 运行基准。
- 已完成 `link_rules` 安全回放基线和统一比较表。
- 已生成本地 reranker → API 兜底的开发集分流曲线。
- 仍待两人独立人工标注，暂不进入模型微调。

## 优先级 P0：统一基线

- [ ] 实现并统计 `link_rules` 基线。
- [ ] 在同一 dev/test 集比较纯规则、规则+link_rules、本地 reranker、API、融合方案。
- [ ] 固定指标：Top-1、错误关联率、歧义拒答率、误拒率、提示注入劫持率、延迟、调用成本。
- [ ] 生成准确率—拒答率—API调用量曲线。

## 优先级 P0：人工标注

- [ ] 从 `real_annotation_queue.jsonl` 选取难负样本和歧义样本。
- [ ] 两名成员独立标注 gold_refs、gold_action、category。
- [ ] 记录分歧和仲裁结果，按文档组划分 dev/test。
- [ ] 只有 `adjudicated=true` 的样本进入正式训练或最终测试。

## 优先级 P1：本地模型

- [ ] 保留 CPU 默认，CUDA 作为可选后端。
- [ ] 若要追求 GPU 加速，评估 FP16 ONNX 或 GPU 友好的模型导出，不能直接假设 INT8 更快。
- [ ] 本地模型低置信度自动拒答，零候选转 API 或人工。
- [ ] 记录模型版本、provider、批量大小、延迟和内存。

## 优先级 P1：是否训练

- [ ] 至少积累人工确认的正样本和同字段难负样本。
- [ ] 采用小型 cross-encoder 的 LoRA/QLoRA 或轻量分类头。
- [ ] 严格按文档组隔离训练、验证、测试。
- [ ] 与未训练 reranker、link_rules 和 API 做增量比较。

## 优先级 P2：产品扩展

- [ ] 模型驱动论断抽取。
- [ ] OCR 图片文字提取后的证据锚点。
- [ ] 多模态图表理解。
- [ ] 更复杂 PPT 图表、页眉页脚和 WPS 兼容验证。
