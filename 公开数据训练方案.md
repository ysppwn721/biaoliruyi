# 公开数据增强训练方案

## 结论

可以从网上获取公开文档和表格来扩充训练数据，最适合训练的是本地事实来源 reranker，不建议用这些资料从零训练大语言模型。公开数据用于训练和难负样本扩充，项目人工复核集用于最终验证，二者必须按组织/文档组隔离。

## 推荐来源

第一批已经可以开始，但先只使用许可证明确的 FinQA、TAT-QA、PubTables-1M 和 DocBank。ChartQA 的 GPL-3.0 和 ToTTo 的许可证状态需要单独处理，不进入当前训练主线。

第一批 FinQA 和 TAT-QA 已完成本地下载与转换：共 8,012 个结构化记录，生成 169,075 条候选对，其中正样本 26,539 条、难负样本 142,536 条。转换结果位于 `答辩评测/public_reranker_pairs.jsonl`。这些是弱标注训练候选，不能直接当成最终人工真值；TAT-QA 的部分表格答案通过公开答案值定位单元格，重复值可能产生多个候选，训练前仍需抽查。

去重后实际保留 129,146 条候选对（正样本 26,539、难负样本 102,607）。结构审计已通过：JSONL 完整、无重复 pair、无 train/dev 组泄漏、事实文本无 `value` 字段。由于这些仍是弱标注数据，已生成分层抽样表 `答辩评测/public_pairs_sample_for_review.csv`，建议先抽查其 200 条再训练。

当前环境没有 PyTorch、Transformers 或 scikit-learn，因此暂不安装大型训练依赖。已准备一个无依赖的词汇重排基线 `答辩评测/public_lexical_baseline.py`，用于先确认公开数据的基本可学习性；后续抽样通过后再安装 PyTorch/Transformers，或导出小型模型训练环境。

词汇基线结果：公开数据 dev 的 Top-1=69.75%，错误关联率=29.25%，误拒率=1.01%；train 的 Top-1=70.05%。这说明任务有可学习的词汇信号，但仍有大量同指标/同期间难负样本，直接使用现成规则或词法匹配不够，训练 reranker 有价值。结果见 `答辩评测/public_lexical_baseline_metrics.json`。

第一步数据审计已经开始，审计结果写入 `答辩评测/public_pairs_audit.json`。只有通过字段完整性、重复样本、train/dev 组隔离和事实元数据数值泄漏检查后，才进入模型训练。

### 中文业务数据

| 来源 | 可获取内容 | 适合用途 | 注意事项 |
| --- | --- | --- | --- |
| [国家数据](https://data.stats.gov.cn/) | 指标、地区、年度/月度统计数据 | 主体、指标、期间、单位匹配 | 核对网站使用条款和数据更新时间 |
| [国家统计局](https://www.stats.gov.cn/) | 统计公报、年鉴、表格和说明文字 | 中文报告—表格配对 | PDF/网页表格需保留原始链接和发布日期 |
| [上海证券交易所](https://www.sse.com.cn/) | 上市公司公告、年报 | 财务论断和表格来源 | 只使用公开下载内容，记录公告链接和年份 |
| [深圳证券交易所](https://www.szse.cn/) | 上市公司公告、年报 | 财务论断和表格来源 | 检查下载和再分发限制 |
| [中国政府网](https://www.gov.cn/) | 政府报告、统计公报、政策表格 | 长文本和阈值/比例论断 | 只保留必要文本，记录原文出处 |

### 国际公开数据集

| 数据集 | 内容 | 适合用途 | 局限 |
| --- | --- | --- | --- |
| [ToTTo](https://github.com/google-research-datasets/ToTTo) | 表格到文字生成 | 表格—文本论断配对 | 不是中文跨 Office 来源关联 |
| [FinQA](https://github.com/czyssrs/FinQA) | 财务报告、表格和问答 | 财务表格理解 | 多为英文问答，不可直接当最终测试 |
| [TAT-QA](https://github.com/NExTplusplus/TAT-QA) | 财务表格和文本推理 | 表格—文本关系、难负样本 | 许可证和任务定义需单独核对 |
| [ChartQA](https://github.com/vis-nlp/ChartQA) | 图表问答 | 图表理解扩展 | 重点是问答，不是来源 ID 关联 |
| [PubTables-1M](https://github.com/microsoft/table-transformer) | 科学文档表格结构 | 表格解析预训练 | 不能直接证明业务语义匹配 |
| [DocBank](https://github.com/doc-analysis/DocBank) | 文档布局标注 | PDF/文档结构解析 | 与来源关联任务不同 |

公开数据集的许可证各不相同。下载前需要记录许可证、原始 URL、下载日期、是否允许修改和再分发；如果无法确认许可证，就只在本地实验使用，不放入提交包。

## 数据加工格式

每个文档组应包含：

1. 一份原始文档或报告。
2. 一份配套 CSV/XLSX/HTML 表格。
3. 一个事实元数据表：`id、subject、metric、period、unit、scope`。
4. 一组论断—事实关联：`claim_text、gold_refs、gold_action、category、source_url、license`。

训练时将事实数值从 `fact_text` 中去掉，只保留主体、指标、期间、单位和口径，与知链线上推理输入保持一致。论断中的数字可以保留，因为它是原始论断文本的一部分；但要避免把同一文档的重复变体同时放进训练和测试。

## 如何自动构造样本

### 正样本

- 报告正文明确指向表格行或表头的论断。
- 同一文档中指标、主体、期间、单位和口径完全一致的事实。
- 财务报告中的同比、环比、预算比较和排名关系。

### 难负样本

- 同指标、不同期间。
- 同期间、不同主体。
- 同主体和指标、不同统计口径。
- 单位不同，例如元和万元。
- 当前值与上期值互换。
- 同一论断需要两个事实，却只给一个事实。

### 必须人工确认的样本

- 表格中存在多个相同指标和期间。
- PDF/OCR 表格结构不完整。
- 文本只表达因果或背景，没有明确数值关系。
- 来源链接只能通过猜测得到。

## 训练/验证/测试划分

按公司、机构或报告组划分，不能按句子随机划分：

- 训练集：70%，允许使用弱标注和人工修正样本。
- 验证集：15%，用于阈值、margin 和难负样本比例调整。
- 测试集：15%，冻结后不能参与调参。
- 项目自己的人工复核集单独保留，作为跨来源回归集。

第一阶段建议目标：至少 500 个独立文档组、5000 个正样本、15000 个难负样本。达不到这个规模时，继续使用现成 `bge-reranker-v2-m3`，不要急于微调。

## 推荐技术路线

1. 先采集 50 个许可明确的文档组做管线验证。
2. 自动抽取表格和论断，生成候选对。
3. 用规则生成弱标签，再人工抽查 10%–20%。
4. 训练/验证只使用事实元数据，不使用事实数值。
5. 与未训练 reranker、纯规则、link_rules 和 API 对比。
6. 只有当 test 集错误关联率下降且误拒率没有明显上升时，才保留训练模型。

## 不建议的做法

- 直接把网页 HTML 全量抓取后训练。
- 把搜索结果摘要、论坛内容或无许可证文件放入提交包。
- 把同一公司同一年报告的相似段落随机拆到 train/test。
- 用项目规则自动生成的标签宣称“真实行业准确率”。
- 训练后删除来源 URL、许可证和原始文件哈希。
