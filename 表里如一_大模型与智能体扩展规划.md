# 「表里如一」大模型与智能体方向扩展规划

> 基于对项目全部源码的通读与实测，针对「大模型与智能体应用」赛题方向的功能扩展与优化建议。

---

## 一、当前定位：一个刻意"压着模型用"的系统

当前架构中，模型只承担三件事：**来源关联建议、诊断解释润色、OCR 识别**，且遵循"不计算、不裁决、不写文件"的铁律。

这套"**确定性优先 + 人工闸门**"的设计是差异化优势——可审计、防幻觉，答辩时讲治理范式很加分。但在"大模型与智能体应用"赛题下有个现实风险：**评委可能质疑模型含量太低**——Agent 是固定状态机，模型是单轮建议工具。

因此扩展的主线应该是：**在不破坏"确定性核验 + 人工闸门"原则的前提下，逐步扩大模型的职责边界**。每一项扩展都要能讲出"模型生成 → 确定性校验 → 人工批准"的闭环故事。

---

## 二、功能扩展方向（按性价比排序）

### ① 模型驱动的自然语言论断抽取（最高优先级）

这是项目最大的能力缺口：规则只认五种固定句式，真实报告里的占比、同比环比、平均值、区间、多主体对比、趋势表述全部识别不了（实测"本期支出80万元，占预算的80%"直接漏掉）。

扩展方式：

- 模型负责**抽取**：把任意结论性表述转成结构化论断（类型/实体/数值/方向/置信度）；
- 确定性引擎继续负责**计算**；
- 抽取结果进入和现在完全相同的"待人工确认"流程。

这样模型用量变深了，但安全模型没变——这是最好讲的答辩故事。

配套设计：**置信度分级**——高置信直接进待确认列表，低置信进"疑似论断"区。

### ② 建立模型评测体系（答辩杀伤力最大）

现在自动测试全用 mock，README 也承认"模型质量需配置密钥后验证"。建议构造一个 100~200 组的评测集（论断-事实配对 + 歧义样本 + 注入对抗样本），产出三个数字：

1. 关联建议 Top-1 准确率；
2. 规则+模型融合相对纯规则的提升；
3. 对抗样本劫持率。

> "我们的模型建议比纯规则候选准确率高 X%，且在文档内嵌'请忽略以上指令'的对抗样本下零劫持"——这一句话顶十页 PPT。

对抗测试本身（往 Word 里埋 prompt 注入语句）就是现成的演示素材。

### ③ Agent 架构升级：固定状态机 → 受限计划-执行

`agent.py` 的 TOOLS 注册表和 `_tool` 轨迹机制已经把地基打好，升级路径很顺：

- 让模型在诊断前先**生成核验计划**（哪些论断优先、哪些需要消歧、建议的检查顺序），再由状态机执行；
- 只读工具自动执行，写操作仍走人工闸门。

答辩被问"这算 Agent 还是 workflow"时，可以从"**确定性骨架保证安全 + 模型负责规划与解释**"来定义"可靠智能体"——这比纯 ReAct 更适合治理场景的叙事。

代码注释已预留 `analyze_ambiguity`、`summarize` 两个扩展点，**消歧助手**（模型分析"这句话的销售额更可能指哪个口径"并给理由）是现成的下一步。

### ④ 项目内 RAG：核验问答与审计摘要

范围小、好做、演示效果好：对单个项目的核验记录 + 审计轨迹 + 报告原文做问答（"这次改了哪些数字？为什么第 3 页结论失效？哪些还没确认？"）。不涉及外部知识库，检索就是项目 JSON，半天能出 demo。

再加一个**变更报告的执行摘要**：模型生成，数字字段全部引用确定性计算结果，沿用 `explain_diagnosis` 已有的防编造数字校验。

### ⑤ 多模态扩展：从"图片文字"到"图表理解"

OCR 雏形已有，顺势扩展两条：

- 拍照/截图里的**柱状图数值识别** → 转成 chart 论断进入只读核验；
- **PDF 报告支持**（解析文本层 + 扫描页走 OCR）。

真实业务里报告一大半是 PDF，这是实用性最直接的缺口。

### ⑥ 智能体记忆：让关联建议越用越准

`link_rules` 已有"记住同类论断的上次选择"，升级成把**历史人工确认记录作为 few-shot 上下文**注入建议请求（项目内优先、跨项目泛化）。这是最自然的"智能体记忆"故事，且完全复用已有数据。

---

## 三、架构层面的优化方向

| 方向 | 现状 | 建议 |
| --- | --- | --- |
| 执行模型 | 同步 + 全局锁，模型请求期间其他项目写操作等待 | 后台任务化（async 队列）+ 进度上报 + 可取消 |
| 成本可观测 | 轨迹只记耗时 | 审计里加 token 用量（DeepSeek 响应含 usage 字段），按项目/按次统计——治理系统自己也要"可审计" |
| 模型调用 | 单轮、40 论断/150 事实上限 | 分批 + 结果缓存（同论断不重复请求；已有"等待期间重复运行不重新请求"逻辑，可推广） |
| 上下文工程 | 只发元数据 | 注入历史确认记录（⑥）+ 明确的输出 schema 校验（已有），逐步形成"受限事实匹配"的 prompt 资产 |
| 多智能体生态 | MCP server 只读 7 工具已完成 | 反向打通：知链 Agent 的 `_tool` 接 MCP client，让外部 Agent（Claude/Cursor）成为可选执行器，讲"智能体互操作"故事 |

---

## 四、建议的路线图

| 阶段 | 内容 |
| --- | --- |
| **决赛前（1-2 周量级）** | ② 评测集 + 真实模型跑分（不改产品代码，纯加分项）；④ 核验问答（小）；① 最小版——模型抽取只做"高置信句式的规则补充"，低风险快速见效 |
| **中期（1-2 月）** | ① 完整版论断抽取 + 置信度分级；③ 计划-执行 Agent；⑤ PDF/图表识别 |
| **长期** | ⑥ 记忆学习、MCP 互操作、多人协作审阅 |

---

## 五、一句话总结

项目已经把"**值得信任**"做得很扎实，接下来要在"**能干更多**"上做文章——而每一步扩展都坚持"**模型建议、机器校验、人拍板**"，这本身就是这个赛题下最好讲的技术叙事。
