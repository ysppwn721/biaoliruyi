# 知链项目交接文档

> 面向接手本项目的队友和下一位 agent。请先读完本文件，再开始修改或演示。

## 1. 项目定位

知链是一个本地运行的跨文档结论验证与增量修复原型。用户导入一份 Excel 事实表和一份或多份 Word/PPT 成果文件，系统从成果文件中识别受支持的数值论断，人工确认论断与事实 ID 的关系；事实数值发生变化后，系统重新检查论断，预览受影响内容，确认后只修改失效论断并重新读取 Office 文件复核，最后下载更新后的 Office 文件和核验记录。

项目当前版本是 `0.2.0`。它是确定性规则系统加人工确认闭环，不是通用文档理解器，也不会自动理解任意因果、主观或自然语言结论。

当前交接目标：保持现有 Word/PPT/Excel 工作流可用，并在后续继续做真实文件兼容性、长文 Word 和 20 页以上 PPT 的验证。当前轮已完成长文 Word 压力测试，PPT 长页数测试还没有完成。

## 2. 目录和职责

```text
zhilian/app.py       FastAPI 应用、HTTP API、上传限制、认证、静态页面挂载
zhilian/store.py     本地项目存储、版本/快照、SHA 校验、事务性修复、撤销、导出
zhilian/engine.py    论断规则抽取、Decimal 计算、单位换算、状态检查、未覆盖文本
zhilian/graph.py     事实依赖图：派生事实、传递闭包、环检测、级联重算、影响半径
zhilian/office.py    OOXML 读取和写回；Excel/Word/PPT 解析；Word run 级补丁和 PPT 图表更新
zhilian/llm.py       可选 OpenAI 兼容 chat/completions 关联建议适配器
zhilian/demo.py      生成可重复的演示 Excel、Word、PPT
web/index.html       单页工作台 HTML
web/app.js           前端状态、上传、确认关联、更新、修复、导出和撤销流程
web/style.css       页面样式
run.py               按环境变量启动 uvicorn
start.ps1/start.bat Windows 启动入口
tests/               pytest 回归测试及 tests/manual_long_word.py 长文 Word 基准
.zhilian/            本地项目数据和文件版本；是运行时数据，不是源码
```

根目录中还有演示和交接资料：

- `README.md`：用户和开发者总说明，包含支持范围、API、部署和限制。
- `知链事实表模板.xlsx`：事实表模板。
- `测试文件_20260918/`：初始演示输入文件。
- `测试结果_20260918/演示项目_版本7/`：已经跑过一次较大数据变更并修复的演示结果。
- `长文Word测试/`：长文 Word 输入、更新 Excel、修复结果和说明。
- `长文Word测试_整包.zip`：上述长文 Word 测试文件打包。
- `THIRD_PARTY_NOTICES.md`：第三方依赖和许可说明。

当前目录不是 Git 工作树；接手时不要依赖 `git status` 或提交历史判断状态。若后续需要团队协作，建议先把项目纳入版本控制，并把 `.zhilian/`、临时渲染产物和本地密钥排除在源码提交之外。

## 3. 本机运行

环境已创建在项目根目录的 `.venv`。依赖版本见 `requirements-lock.txt`，当前验证环境为 Windows + Python 3.12。

正常启动：

```powershell
Set-Location 'C:\Users\Administrator\Desktop\华北五省设计'
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\start.ps1
```

`start.ps1` 会运行 `run.py`，默认地址为 `http://127.0.0.1:8765`。也可以直接运行：

```powershell
.\.venv\Scripts\python.exe run.py
```

`run.py` 从 `ZHILIAN_HOST`、`ZHILIAN_PORT` 读取监听地址和端口，默认值为 `127.0.0.1:8765`。本轮为了避开已有进程，手动启动过 `127.0.0.1:8767`；交接时先访问：

```text
http://127.0.0.1:8767
```

如需确认服务：

```powershell
Invoke-RestMethod http://127.0.0.1:8767/api/health
```

健康响应应包含 `status: ok`、`version: 0.2.0`。如果使用正常启动脚本，则把端口替换为 8765。不要为了换端口修改前端代码，前端使用当前页面的相对 `/api/...` 地址。

### 数据目录

默认数据目录是项目根目录 `.zhilian/`，也可通过 `ZHILIAN_DATA_DIR` 覆盖。测试和基准都使用临时目录，避免修改真实项目。启动多个 worker 共享同一个数据目录是不安全的；`Store` 只提供进程内锁和本地 JSON/文件版本。

`.env` 可配置：

```text
ZHILIAN_HOST=127.0.0.1
ZHILIAN_PORT=8765
ZHILIAN_ACCESS_PASSWORD=
ZHILIAN_LLM_PROVIDER=
ZHILIAN_LLM_BASE_URL=
ZHILIAN_LLM_MODEL=
ZHILIAN_LLM_API_KEY=
```

不要把真实 API Key 写进聊天、前端、源码或交接文档。模型是可选项，空配置下规则模式可完整运行。

## 4. 用户操作流程

### 新建项目

一次提交至少需要一份 `.xlsx` 和一份 `.docx` 或 `.pptx`，最多 10 个文件、总大小不超过 30MB。前端支持分多次选择文件，后续选择会追加到待上传列表，提交前可移除单个文件。Excel 必须包含以下列，列顺序可以变化：

```text
事实ID、主体、指标、期间、数值、单位、统计口径
```

事实 ID 必须唯一。公式单元格会被拒绝，必须保存为已核实的数值。每个事实表最多 2000 行；成果文件的文本块也有 2000 个限制。

### 关联确认

导入后系统抽取论断并给出候选 `refs`。这些关联不能直接当作事实，用户必须逐项确认，或批量确认后审阅有歧义的条目。只有 `confirmed=true` 的论断才允许修复。

### 更新 Excel

Excel/WPS 中修改并保存后，使用原项目的“导入更新后的 Excel”入口。网页不监听磁盘，不会自动读取已经打开的文件，也不会自动改写已下载的 Word/PPT。

每次更新只选择一个保存后的 `.xlsx`：

1. 上传一个新 Excel。
2. 先看差异预览；预览不会写入项目版本。
3. 确认后导入更新表，建立新的源表 generation 并重新验证。
4. 打开“预览修复”，审阅失效论断的前后文。
5. 确认修复并下载新版 Word/PPT。

不要把两个不同版本的 Excel 同时作为同一批更新文件上传。更新入口要求事实 ID 集合和主体、指标、期间、单位、统计口径都保持不变；只能改变数值或调整行顺序。需要增删事实或改变含义时，重新建立项目并重新确认来源。

### 撤销和导出

“撤销”只恢复上一次数据导入或修复之前的项目状态。导出包包含当前 Excel、Word/PPT、`知链核验记录.json` 和 `核验报告.md`。文件下载前会再次做 SHA 校验。

## 5. 主要 API

所有项目接口的 `{wid}` 是 32 位小写十六进制项目 ID；写接口必须带当前 `revision`。revision 不匹配会返回“项目已在其他窗口更新，请刷新后重试”。

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/health` | 健康检查和模型配置状态 |
| GET | `/api/projects` | 列出本地项目 |
| POST | `/api/projects/demo` | 创建演示项目 |
| GET | `/api/template` | 下载事实表模板 |
| POST | `/api/projects` | multipart 创建项目，字段 `name`、重复 `files` |
| GET | `/api/projects/{wid}` | 获取项目、论断、事实、检查状态和未覆盖文本 |
| POST | `/api/projects/{wid}/links` | JSON `{revision, links:[{claim_id, refs}]}` 确认来源 |
| POST | `/api/projects/{wid}/facts` | JSON `{revision, values:{fact_id:number}}` 直接改数值（只接受基础事实） |
| GET | `/api/projects/{wid}/derivations` | 读取派生定义、拓扑序、环清单与当前派生值 |
| POST | `/api/projects/{wid}/derivations` | JSON `{revision, derivations:[{id,expr,unit,label}]}` 整体替换派生定义 |
| DELETE | `/api/projects/{wid}/derivations/{rid}` | 查询参数 `revision`；被其他派生事实引用时拒绝删除 |
| POST | `/api/projects/{wid}/source/preview` | multipart `revision` + `file`，只预览外部 Excel |
| POST | `/api/projects/{wid}/source` | multipart `revision` + `file`，正式导入外部 Excel |
| POST | `/api/projects/{wid}/repair` | JSON `{revision, claim_ids}` 修复选中失效论断 |
| POST | `/api/projects/{wid}/undo` | JSON `{revision}` 撤销上一次文件变更 |
| POST | `/api/projects/{wid}/suggest` | JSON `{revision}` 请求可选模型关联建议 |
| GET | `/api/projects/{wid}/files/{fid}` | 下载单个当前文件 |
| GET | `/api/projects/{wid}/export` | 下载成果与核验记录 ZIP |

如果设置了 `ZHILIAN_ACCESS_PASSWORD`，Basic Auth 用户名固定为 `zhilian`。所有非 GET/HEAD/OPTIONS 跨站 Origin 会被拒绝；API 响应设置了 no-store、nosniff、same-origin 和 DENY 等基础安全头。

## 6. 状态、版本和修复机制

### 事实依赖图与派生事实

基础事实来自 Excel；**派生事实**由受限表达式从其他事实算出，不写入 Excel、也不能直接编辑。目前支持四种运算（白名单，不接受任意代码或模型生成的表达式）：

```text
sum(a, b, ...)   合计：指标、期间、口径需一致，主体允许不同（A产品 + B产品）
diff(a, b)       差额：只要求主体与口径一致，允许指标不同（销售额 − 成本）
ratio(a, b)      比率：同期不同指标（成本占比）或同指标跨期，输出百分比
growth(a, b)     增长率：严格要求 a=本期、b=上期且同指标，输出百分比
```

派生事实会**并入 `ws['facts']`**，因此论断和图表都可以引用它们；改一个基础事实后，下游派生值按拓扑序重算，`POST /facts` 与 `POST /source` 的响应里会多出一个 `cascade` 字段：

```text
cascade.changed_facts     本次变化的基础事实
cascade.changed_derived   数值真正发生变化的派生事实
cascade.affected_facts    受影响的派生事实（含未变化者）
cascade.direct_claims     直接引用变化事实的论断
cascade.indirect_claims   经依赖图传播后才受影响的论断
cascade.radius            影响半径
```

`derivations` 定义在写入前统一校验：引用不存在、引用自己、重复 ID、循环依赖、sum 少于两项、非白名单运算都会被拒绝并返回可读原因，且**不写入任何状态**。

一个项目目录结构大致如下：

```text
.zhilian/<wid>/
  state.json
  v0/
    <随机文件名>.xlsx
    <随机文件名>.docx
    <随机文件名>.pptx
  v<随机12位hex>/
    当前代文件
    previous-state.json
```

`state.json` 主要字段：

- `revision`：每次确认关联、数据变更、外部 Excel 导入、修复、模型建议或撤销都会递增。
- `generation`：当前文件目录版本。
- `documents`：原始文件名、种类、存储名、SHA256、警告。
- `facts`：Excel 事实记录，**包含派生事实**（带 `derived: true`、`op`、`inputs`）。
- `source_facts`：仅来自 Excel 的基础事实；只在这里的 ID 允许写入。
- `derivations`：派生事实定义（`id`、`op`、`inputs`、`unit`、`label`）。
- `graph`：本轮派生结果——`derived` 值、`errors`（无法计算的原因）、`details`（计算说明）、`changed`、`affected`、`radius`、`cycles`。
- `graph_values`：上一轮值表，用于判断派生值是否真的变化（缺失它会把未变化的派生值误报为变化）。
- `blocks`：Word 段落/表格单元格或 PPT 文本块。
- `claims`：规则抽取的论断、稳定 ID、原文区间、`refs`、确认状态和计算规格。
- `history`、`audit`：可回溯的操作记录。
- `last_repair`：最近一次修复数量、补丁和独立复核结果。

所有写操作先复制旧 generation，写入新 generation，完成重新扫描和验证后才替换当前状态；失败时删除新目录。`verify()` 在写操作前比较 revision 和所有文档 SHA256，防止并发窗口或磁盘文件被外部修改后静默覆盖。

修复流程会：

1. 只接受已确认且当前状态为 `inconsistent` 的 claim。
2. 按原始 `location` 分组，按字符范围逆序替换，避免前面的替换改变后面锚点。
3. Word 使用 run 级补丁，尽量保留未修改文字格式；PPT 普通文本同理，原生单系列柱形图重建数据系列。
4. 重新读取生成的 Office 文件。
5. 验证选中 claim 已一致、其他支持范围内 block 文本未变化。
6. 通过后记录新 generation；失败则不保留新版本。

## 7. 当前识别和编辑范围

`engine.py` 使用 Decimal 和显式单位换算，不使用 `eval` 或模型算数。当前支持：

- 数值引用：`本期销售额为125万元`。
- 增长率：`本期销售额较上期增长25%`、下降、持平；也支持只写方向的定性增长。
- 排名：`A产品销量最高`、并列最高；只对已经确认的比较集合负责。
- 阈值：超过、不超过、不少于、低于，以及“支出未超过预算”。
- PPT 单系列簇状柱形图：按类别和系列关联事实并重建数据。

下列情况会进入人工复核或不纳入自动处理：零/负基期、口径冲突、无法唯一匹配、因果解释、主观意见、图片中的文字、页眉页脚、备注、母版、复杂域、文本框以外复杂对象、复杂图表、组合对象、宏文件、旧式 `.xls/.doc/.ppt`。Word 当前重点是普通正文段落和一层普通表格；PPT 当前重点是普通文本框、表格和单系列簇状柱形图。

`unmatched_segments` 是未被论断锚点覆盖的原文片段，包含标题、背景说明、人工复核内容和句中连接词，数量不能直接解释为错误数量。

## 8. 已完成的长文 Word 验证

`tests/manual_long_word.py` 会构造并用真实 FastAPI `TestClient` 跑完整工作流，不修改真实 `.zhilian` 数据。运行：

```powershell
# 只重建长文 Word 和独立 manifest
& 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' tests/manual_long_word.py --build-only

# 用项目 .venv 跑导入、确认、Excel 更新、修复、下载、撤销
.\.venv\Scripts\python.exe tests/manual_long_word.py
```

结果文件在 `.codex_artifacts/long_word/results.json`，测试材料在 `长文Word测试/`。当前已验证：

- 24 章、约 17,066 字符、337 个段落（含表格单元格）、8 张表。
- 280 项初始论断全部识别且一致。
- 销售额 125→123：80 项失效并成功修复；其余 200 项保持一致。
- 销售额 125→90、A 产品 80→60、支出 80→110：232 项失效并成功修复；其余 48 项保持一致。
- 修复后两种场景均 280 项一致；全文与独立编写的 manifest 一致。
- Word 段落/运行属性和非 `word/document.xml` 的 18 个 ZIP 部件保持不变。
- 原始、轻微变化、严重变化 Word 通过 WPS 原生 PDF 导出均为 24 页，已逐页检查排版。

渲染脚本位于 `.codex_artifacts/long_word/render_native.ps1`。标准 documents renderer 因本机没有 LibreOffice 而无法使用，视觉 QA 采用本机已安装的 WPS COM 导出；这不是项目运行时依赖，也不应写成“使用 LibreOffice 验证”。

这个基准是重复的六事实合成数据，适合验证长文定位、表格和批量补丁，不代表真实行业语义泛化能力。PPT 超过 20 页的专项验证尚未完成。

## 9. 回归测试

完整测试：

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

覆盖内容包括：

- 真实 Word/PPT 修改、原生图表、格式保留和撤销。
- 复合句逐项拆分，数值引用不被阈值结论遮蔽。
- 增长率、排名并列、零基期、口径冲突、歧义来源、缺失证据。
- Excel 公式拒绝、事实表导入、外部更新预览/提交/撤销。
- 并发 revision、磁盘篡改、跨 Origin 请求和 Basic Auth。
- 导出 ZIP、HTTP 上传持久化和模型建议返回结构。

测试是构造数据回归，不等于真实文件兼容性保证。任何 Office 处理逻辑改动后至少运行全量 pytest，并重新跑长文基准；涉及排版时必须重新渲染并检查页面。

## 10. 接手后的建议顺序

1. 先阅读本文件和 `README.md`，访问当前服务的 `/api/health`。
2. 运行 `pytest -q`，确认基线通过。
3. 用演示项目体验一次：确认关联 → 改数值 → 预览修复 → 导出 → 撤销。
4. 用 `长文Word测试/` 按说明逐个上传初始文件和一个更新 Excel，确认 24 页 Word 的修复结果。
5. 再设计 PPT 20 页以上测试，重点测长页数、普通文本框、表格、图表、文本样式和分页/溢出；不要把 Word 结果直接外推到 PPT。
6. 如修改规则或 Office 写回，先加最小回归测试，再修改代码。
7. 如部署给队友，复制 `.env.example` 为 `.env`，设置访问密码，使用单 worker；公网必须通过 HTTPS 反向代理。

## 11. 已知问题和不要误判的地方

- README 默认端口是 8765，本轮手动测试服务使用 8767；以启动命令和 `/api/health` 为准。
- 浏览器真实上传耗时没有做性能承诺，基准里的时间是进程内 TestClient 时间，不包含浏览器和网络。
- 模型适配器目前没有真实 API 质量评测；它只建议来源关联，不能自动确认或改文件。
- 页眉页脚在长文测试里保留，但不属于自动 claim 检查范围。
- 知链只会修复受支持且已确认的失效论断；未识别文本不会因为同段有一条通过就自动变成正确。
- 事实 ID 集合或事实语义变化时，外部 Excel 更新入口会拒绝，这是为了避免沿用错误关联。
- 不要直接编辑 `.zhilian/<wid>/state.json` 或 generation 文件来“修数据”；会破坏 SHA 和版本一致性。
- 不要同时启动多个服务进程写同一个数据目录，也不要在未确认关联的项目上调用修复。

## 12. 交接完成标准

接手者能够在自己的环境中完成以下闭环，才算交接成功：

- 能启动服务并通过 `/api/health`。
- 能创建演示项目并解释 `facts`、`claims`、`refs`、`blocks`、`revision`、`generation` 的关系。
- 能说明为什么更新 Excel 后还需要预览、确认修复和重新下载 Word/PPT。
- 能跑通全量 pytest 和长文 Word 基准。
- 能明确列出当前支持范围和不支持的 Office 对象。
- 修改代码后能提供对应测试和视觉验证证据。
