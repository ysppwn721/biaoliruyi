# 第一批双人标注结果

更新时间：2026-09-25

两个独立子智能体分别读取原始长文 Word、配套数据 Excel 和 64 条候选，未读取对方文件，也未读取 `real_eval_dataset.jsonl`。这属于“双智能体交叉标注”，不能在答辩中表述为两名真人专家标注；若比赛明确要求人工标注，仍需由两名队员按同一流程复核。

| 项目 | 结果 |
| --- | ---: |
| 标注样本 | 64 |
| A/B 决策一致 | 64 |
| A/B 决策分歧 | 0 |
| 一致率 | 100% |
| `gold_action=link` | 64 |
| `gold_action=abstain` | 0 |
| `category=ordinary` | 64 |
| `category=ambiguous/adversarial` | 0 |
| 已仲裁 | 64 |

11 条“本期支出超过100万元”类论断被双方标为来源唯一，但备注指出事实表中的支出为80万元，属于数值与文字阈值不一致，应由确定性引擎诊断为失效并进入修复流程；它不是来源歧义。

合并文件：`real_annotation_batch_01_merged.jsonl`。合并时将多事实来源按候选事实表的稳定顺序规范化，例如增长率使用“上期→本期”。

这批样本的 `claim_text` 是抽取后的局部论断，脱离 Word 段落上下文后不能直接用来评估完整规则引擎的来源召回率；它现在作为人工标签质量和标注流程验证集。正式模型评测仍需在保留文档上下文的评测格式上运行。
