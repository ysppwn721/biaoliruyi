# 真实中文年报识别率实测

> 目的：检验引擎在**真实语料**上的表现，而非项目自建合成语料
> 语料：巨潮资讯网（cninfo）2022 年年度报告，169 页，文件 3.55 MB
> 结论：**识别率 0.5%（3/580）**，与合成语料上的 100% 相差约 200 倍

## 一、实测数据

```
文件      : *ST宁科2022年年度报告（样本 1）
页数      : 169
抽取范围  : 第 10—45 页（管理层讨论与分析 / 经营情况讨论与分析）
抽取字符  : 40,008
句子总数  : 1,863
含数字句子: 580        ← 客观分母，不依赖任何规则
```

| 指标 | 改规则前 | 改规则后 |
| --- | --- | --- |
| 引擎识别出论断的句子 | **0** | **3** |
| 识别率 | **0%** | **0.5%** |
| 论断类型 | — | quote×1、growth×4 |

**必须如实说明**：识别率仍只有 0.5%。本项目自建合成语料上的 100% 是
"语料按引擎规则撰写"的结果，**不能作为真实泛化能力的证据**。

## 二、已修复的两类表达（0 → 3 条）

### 1. 千分位数值

真实年报普遍写作 `3,379.37 万元`，旧规则只认连续数字，因此整类数值引用全部漏掉。

```python
# 修复前
NUM = r'-?\d+(?:\.\d+)?'
# 修复后
NUM = r'-?\d{1,3}(?:,\d{3})+(?:\.\d+)?|-?\d+(?:\.\d+)?'
```

同时 `number()` 增加千分位剥离，且**仅当逗号按三位分组时才剥离**，
避免把 `1,23` 这类异常输入静默改成一个别的数。

### 2. 增长率用词变体

| 年报写法 | 旧规则 | 新规则 |
| --- | --- | --- |
| `较上期增长25%` | ✅ | ✅ |
| `同比增长25%` | ❌ | ✅ |
| `较上年增加553.21%` | ❌ | ✅ |
| `较上年减少12.5%` | ❌ | ✅ |
| `比上年上升8%` | ❌ | ✅ |

关键修正是**动词变体**：年报用「增加/减少/上升」，旧规则只认「增长/下降/持平」。
同时补上方向词映射，否则会把"减少"读成"增长"——这类错误比漏识别更危险。

## 三、仍存在的缺口（实测样例）

以下都是真实年报里的数值结论，当前**仍识别不到**：

| 样例 | 缺口原因 |
| --- | --- |
| `营业收入占比已超过公司全部营业收入的50%以上` | 算子与数值之间隔着长定语 |
| `公司产品长链二元酸产量增加179.79%` | 无期间限定词的裸「增加」 |
| `本期毛利率低于去年同期` | 与历史期间比较但无数值 |
| `生物基、淀粉基新材增加187.44` | 表格残片，无单位 |

这些缺口已写入 `tests/test_real_phrasing.py`，以"断言识别不到"的形式固定。
一旦后续补齐，测试会失败并提醒同步更新文档与评测口径。

## 四、对训练数据与评测口径的影响

### 1. 合成语料的定位必须改口径

`real_eval_dataset.jsonl`（280 条）来自**确定性引擎重放**，其语料按引擎规则撰写。
它只能验证**实现正确性与回归**，**不能用于报告识别率或泛化能力**。

建议在设计文档与答辩材料中明确写成：

> 280 条为可复现回归夹具（fixture），用于验证实现一致性；
> 真实语料识别率单独实测，见本节数据。

### 2. 训练对来源的建议

从年报构建中文训练对时，**不能直接复用引擎规则**去标注——
0.5% 的识别率意味着规则根本标不出真实语料的样本。

正确做法：

1. 从年报**表格**抽事实（表格抽取已验证可行，见下节）
2. 用**人工或大模型**标注叙述文字中的规格化论断
3. 训练对由"人工确认的论断 + 表格事实"构成

### 3. PDF 表格抽取已验证可行

同一份年报的表格抽取结果（`pdfplumber.extract_tables()`）：

```
第 13 页表: 科目        | 本期数          | 上年同期数      | 变动比例（%）
            营业收入    | 604,539,544.17 | 248,717,404.89 | 143.06
            营业成本    | 604,030,284.29 | 330,631,103.93 | 82.69
```

**结构规整，可直接映射为引擎的事实表**（主体/期间/数值/单位）。
且「变动比例」列可用来**独立校验**引擎算出的增长率——这是很好的交叉验证素材。

## 五、复现命令

```powershell
# 1. 检索并下载年报
curl.exe -s -X POST "http://www.cninfo.com.cn/new/hisAnnouncement/query" `
  -H "Content-Type: application/x-www-form-urlencoded" -H "User-Agent: Mozilla/5.0" `
  --data "pageNum=1&pageSize=8&column=szse&category=category_ndbg_szsh&seDate=2024-06-01~2024-12-31"

# 2. 测量引擎识别率
.\.venv\Scripts\python.exe 答辩评测/measure_engine_on_annual_report.py <pdf> 9 45

# 3. 表达方式回归测试
.\.venv\Scripts\python.exe -m pytest tests/test_real_phrasing.py -q
```
