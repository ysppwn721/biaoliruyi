# 知链优化执行清单进度

更新时间：2026-09-25

## 已完成

- [x] CUDA/CPU 本地 reranker 运行验证：CUDA provider 可用，但当前 INT8 图 CPU 更快。
- [x] 规则基线、`link_rules` 辅助回放、本地 reranker、DeepSeek API 统一指标表。
- [x] 本地 reranker 阈值扫描和 API 兜底调用量估算。
- [x] 长文夹具 280 条数据的开发/测试结果整理。
- [x] 第一批人工标注样本：5 个文档组、64 条候选。
- [x] 第一批双人独立标注：64/64 一致，0 条分歧，已合并并规范化多事实顺序。
- [x] 主项目全量回归：233 passed，1 warning（2026-09-26 以分级路由为准重新校准了 3 个模型路径用例，新增 1 个路由用例）。

## 当前关键结果

- `link_rules` 只能提升已有候选的审阅效率，不能替换规则候选或绕过人工闸门。
- seed 开发集 raw=-3.5、margin=0.3 时，本地 reranker 自动接受率 83.33%，估算 API 兜底 20/120 条；通过阈值的非歧义候选 Top-1 为 100%，歧义拒答率 100%。
- 长文 API 夹具整体 Top-1 86.8%，test 留出组 Top-1 71.4%；该数据是本地夹具，不是行业泛化。

## 已决定不再阻塞交付的人工环节

- [x] 两名成员分别标注 `real_annotation_batch_01.jsonl`。
- [x] 合并并检查第一批结果；无分歧样本需要仲裁。
- [x] 覆盖其余 20 个文档组；216/216 双人一致。
- [x] 合并为完整 `real_human_eval_dataset.jsonl`，共280条、25组。
- [x] 在人工集上运行规则和本地 reranker 首轮回归。
- [x] 从人工集生成不含数值的 reranker 正负训练对；尚未训练模型。
- [x] 完成公开数据增强训练方案和来源登记表；尚未抓取未经许可的外部数据。
- [x] 下载并固定 FinQA/TAT-QA 的 MIT 版本，生成并去重公开弱标注训练候选。
- [x] 保存公开数据文件哈希和 commit；尚未把公开原始数据放入源码交付包。
- [x] 完成公开训练对结构审计：去重后129,146条，无组泄漏、无 `value` 字段。
- [x] 生成公开训练对分层抽样复核表：200条，覆盖两个数据集和 train/dev、正负样本。
- [x] 生成公开数据词汇重排基线脚本；尚未安装 PyTorch/Transformers，未开始微调。
- [x] 运行公开数据词汇基线：dev Top-1=69.75%，错误关联率=29.25%。

> 说明：不再安排队员人工复核。`real_human_eval_dataset.jsonl` 的标签统一称为“双智能体一致性检查”，公开年报集统一称为“程序化 PDF 外部压力集”，答辩材料不得称为真人双标或人工真值。

## 当前交付决策

- [x] 冻结程序化 PDF 外部压力集：36 份年报、576 条论断、1,728 个候选对、36 个文档组。
- [x] 选择 BGE ONNX 作为线上默认本地模型：外部程序化集 Top-1=96.18%。
- [x] 保留中文 BERT v2 为研究模型：外部程序化集 Top-1=41.32%，暂不替换线上模型。
- [x] 停止继续用当前合成数据微调 BERT；新增真实中文文档组前不再训练。
- [x] 更新 PPT 文案、外部验证图、数据表和交接说明。

## 最终收尾步骤

1. 运行全量 pytest 和长文 Word 基准，保存通过记录。
2. 检查 BGE 默认配置、无模型降级和 API 可选兜底路径。
3. 将源码、requirements、评测指标、PPT 文案和图表整理为最终交付包。
4. 答辩中区分冻结测试集、双智能体一致性检查、程序化 PDF 外部集三种口径。

## 后续升级条件

只有在新增真实中文文档组并获得明确标注后，才重新划分 train/dev/test 并训练小型 cross-encoder；当前版本继续使用 BGE + 规则收紧候选 + 低置信度转 API/人工的组合。
